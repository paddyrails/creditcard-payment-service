# creditcard-payment-service
Credit Card Management - Payment Processing Service
## Build: `mvn spring-boot:run` or `docker build -t creditcard-payment-service . && docker run -p 8084:8080 creditcard-payment-service`
## API: http://localhost:8084/swagger-ui.html
